import 'package:flutter/material.dart';
class ActividadesHomePage extends StatelessWidget {
  const ActividadesHomePage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.white,
      child: Center(child:Text('Actividades')),
    );
  }
}